    model = net = torch.nn.Sequential(
                    torch.nn.Linear(4, 32),
                    torch.nn.LeakyReLU(),
                    torch.nn.Linear(32, 64),
                    torch.nn.LeakyReLU(),
                    torch.nn.Linear(64, 4),
                    torch.nn.LeakyReLU(),
                    torch.nn.Linear(4, 1),
                   )
