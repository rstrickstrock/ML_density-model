    model = net = torch.nn.Sequential(
                    torch.nn.Linear(4, 64),
                    torch.nn.LeakyReLU(),
                    torch.nn.Linear(64, 8),
                    torch.nn.LeakyReLU(),
                    torch.nn.Linear(8, 1),
                   )
